import{_ as s,c as n,o as a,d as e}from"./app.ba9181fa.js";const h='{"title":"Getting Started","description":"","frontmatter":{},"headers":[{"level":2,"title":"Your First Jasper Application","slug":"your-first-jasper-application"}],"relativePath":"guide/index.md","lastUpdated":1655492219641}',t={},o=e(`<h1 id="getting-started" tabindex="-1">Getting Started <a class="header-anchor" href="#getting-started" aria-hidden="true">#</a></h1><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Jasper targets .Net 6 and above.</p></div><p>Jasper is a toolset for command execution and message handling within .Net Core applications. The killer feature of Jasper (we think) is its very efficient command execution pipeline that can be used as:</p><ol><li>An inline &quot;mediator&quot; pipeline for executing commands</li><li>An in memory messaging bus and command executor within .Net applications</li><li>When used in conjunction with low level messaging infrastructure tools like RabbitMQ, a full fledged asynchronous messaging platform for robust communication and interaction between services</li></ol><p>Jasper tries very hard to be a good citizen within the .Net ecosystem and even when used in &quot;headless&quot; services, uses many elements of ASP.Net Core (logging, configuration, bootstrapping, hosted services) rather than try to reinvent something new. Jasper utilizes the new .Net Generic Host for bootstrapping and application teardown. This makes Jasper relatively easy to use in combination with many of the most popular .Net tools.</p><h2 id="your-first-jasper-application" tabindex="-1">Your First Jasper Application <a class="header-anchor" href="#your-first-jasper-application" aria-hidden="true">#</a></h2><p>Also see the full <a href="https://github.com/JasperFx/jasper/tree/master/src/Samples/Quickstart" target="_blank" rel="noopener noreferrer">quickstart code</a> on GitHub.</p><p>For a first application, let&#39;s say that we&#39;re building a very simple issue tracking system for our own usage. If you&#39;re reading this web page, it&#39;s a pretty safe bet you spend quite a bit of time working with an issue tracking system:)</p><p>Ignoring any discussion of the user interface or even a backing database, let&#39;s start a new web api project for this new system with:</p><div class="language-bash"><pre><code>dotnet new webapi
</code></pre></div><p>Next, let&#39;s add Jasper to our project with:</p><div class="language-bash"><pre><code>dotnet <span class="token function">add</span> package Jasper
</code></pre></div><p>To start off, we&#39;re just going to build two API endpoints that accepts a POST from the client that...</p><ol><li>Creates a new <code>Issue</code>, stores it, and triggers an email to internal personal.</li><li>Assigns an <code>Issue</code> to an existing <code>User</code> and triggers an email to that user letting them know there&#39;s more work on their plate</li></ol><p>The two <em>commands</em> for the POST endpoints are below:</p><p><a id="snippet-sample_quickstart_commands"></a></p><div class="language-cs"><pre><code><span class="token keyword">public</span> <span class="token keyword">record</span> <span class="token class-name">CreateIssue</span><span class="token punctuation">(</span><span class="token class-name">Guid</span> OriginatorId<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> Title<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> Description<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p><sup><a href="https://github.com/JasperFx/alba/blob/master/src/KitchenSink/MartenAndRabbitMessages/CreateIssue.cs#L3-L7" title="Snippet source file">snippet source</a> | <a href="#snippet-sample_quickstart_commands" title="Start of snippet">anchor</a></sup><a id="snippet-sample_quickstart_commands-1"></a></p><div class="language-cs"><pre><code><span class="token keyword">public</span> <span class="token keyword">record</span> <span class="token class-name">CreateIssue</span><span class="token punctuation">(</span><span class="token class-name">Guid</span> OriginatorId<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> Title<span class="token punctuation">,</span> <span class="token class-name"><span class="token keyword">string</span></span> Description<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p><sup><a href="https://github.com/JasperFx/alba/blob/master/src/Samples/Quickstart/CreateIssue.cs#L3-L7" title="Snippet source file">snippet source</a> | <a href="#snippet-sample_quickstart_commands-1" title="Start of snippet">anchor</a></sup></p><p>Let&#39;s jump right into the <code>Program.cs</code> file of our new web service:</p><p><a id="snippet-sample_quickstart_program"></a></p><div class="language-cs"><pre><code><span class="token keyword">using</span> <span class="token namespace">Jasper</span><span class="token punctuation">;</span>
<span class="token keyword">using</span> <span class="token namespace">Quickstart</span><span class="token punctuation">;</span>

<span class="token class-name"><span class="token keyword">var</span></span> builder <span class="token operator">=</span> WebApplication<span class="token punctuation">.</span><span class="token function">CreateBuilder</span><span class="token punctuation">(</span>args<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// For now, this is enough to integrate Jasper into</span>
<span class="token comment">// your application, but there&#39;ll be *much* more</span>
<span class="token comment">// options later of course :-)</span>
builder<span class="token punctuation">.</span>Host<span class="token punctuation">.</span><span class="token function">UseJasper</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// Some in memory services for our application, the</span>
<span class="token comment">// only thing that matters for now is that these are</span>
<span class="token comment">// systems built by the application&#39;s IoC container</span>
builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddSingleton</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>UserRepository<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
builder<span class="token punctuation">.</span>Services<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">AddSingleton</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IssueRepository<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token class-name"><span class="token keyword">var</span></span> app <span class="token operator">=</span> builder<span class="token punctuation">.</span><span class="token function">Build</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// An endpoint to create a new issue</span>
app<span class="token punctuation">.</span><span class="token function">MapPost</span><span class="token punctuation">(</span><span class="token string">&quot;/issues/create&quot;</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token class-name">CreateIssue</span> body<span class="token punctuation">,</span> <span class="token class-name">ICommandBus</span> bus<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> bus<span class="token punctuation">.</span><span class="token function">InvokeAsync</span><span class="token punctuation">(</span>body<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// An endpoint to assign an issue to an existing user</span>
app<span class="token punctuation">.</span><span class="token function">MapPost</span><span class="token punctuation">(</span><span class="token string">&quot;/issues/assign&quot;</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token class-name">AssignIssue</span> body<span class="token punctuation">,</span> <span class="token class-name">ICommandBus</span> bus<span class="token punctuation">)</span> <span class="token operator">=&gt;</span> bus<span class="token punctuation">.</span><span class="token function">InvokeAsync</span><span class="token punctuation">(</span>body<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

app<span class="token punctuation">.</span><span class="token function">Run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p><sup><a href="https://github.com/JasperFx/alba/blob/master/src/Samples/Quickstart/Program.cs#L1-L30" title="Snippet source file">snippet source</a> | <a href="#snippet-sample_quickstart_program" title="Start of snippet">anchor</a></sup></p><p>Alright, let&#39;s talk about what&#39;s going on up above:</p><ol><li>I integrated Jasper into the new system through the call to <code>IHostBuilder.UseJasper()</code></li><li>I registered the <code>UserRepository</code> and <code>IssueRepository</code> services</li><li>I created a couple <a href="https://docs.microsoft.com/en-us/aspnet/core/fundamentals/minimal-apis?view=aspnetcore-6.0" target="_blank" rel="noopener noreferrer">Minimal API</a> endpoints</li></ol><p>See also: <a href="/guide/in-memory-bus.html">Jasper as Command Bus</a></p><p>The two Web API functions directly delegate to Jasper&#39;s <code>ICommandBus.InvokeAsync()</code> method. In that method, Jasper will direct the command to the correct handler and invoke that handler inline. In a simplistic form, here is the entire handler file for the <code>CreateIssue</code> command:</p><p><a id="snippet-sample_quickstart_createissuehandler"></a></p><div class="language-cs"><pre><code><span class="token keyword">namespace</span> <span class="token namespace">Quickstart</span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">CreateIssueHandler</span>
<span class="token punctuation">{</span>
    <span class="token keyword">private</span> <span class="token keyword">readonly</span> <span class="token class-name">IssueRepository</span> _repository<span class="token punctuation">;</span>

    <span class="token keyword">public</span> <span class="token function">CreateIssueHandler</span><span class="token punctuation">(</span><span class="token class-name">IssueRepository</span> repository<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        _repository <span class="token operator">=</span> repository<span class="token punctuation">;</span>
    <span class="token punctuation">}</span>

    <span class="token keyword">public</span> <span class="token return-type class-name">IssueCreated</span> <span class="token function">Handle</span><span class="token punctuation">(</span><span class="token class-name">CreateIssue</span> command<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        <span class="token class-name"><span class="token keyword">var</span></span> issue <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">Issue</span>
        <span class="token punctuation">{</span>
            Title <span class="token operator">=</span> command<span class="token punctuation">.</span>Title<span class="token punctuation">,</span>
            Description <span class="token operator">=</span> command<span class="token punctuation">.</span>Description<span class="token punctuation">,</span>
            IsOpen <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
            Opened <span class="token operator">=</span> DateTimeOffset<span class="token punctuation">.</span>Now<span class="token punctuation">,</span>
            OriginatorId <span class="token operator">=</span> command<span class="token punctuation">.</span>OriginatorId
        <span class="token punctuation">}</span><span class="token punctuation">;</span>

        _repository<span class="token punctuation">.</span><span class="token function">Store</span><span class="token punctuation">(</span>issue<span class="token punctuation">)</span><span class="token punctuation">;</span>

        <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">IssueCreated</span><span class="token punctuation">(</span>issue<span class="token punctuation">.</span>Id<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p><sup><a href="https://github.com/JasperFx/alba/blob/master/src/Samples/Quickstart/CreateIssueHandler.cs#L1-L31" title="Snippet source file">snippet source</a> | <a href="#snippet-sample_quickstart_createissuehandler" title="Start of snippet">anchor</a></sup></p><p>Hopefully that code is simple enough, but let&#39;s talk what you do not see in this code or the initial <code>Program</code> code up above.</p><p>Jasper uses a naming convention to automatically discover message handler actions in your application assembly, so at no point did we have to explicitly register the <code>CreateIssueHandler</code> in any way.</p><p>We didn&#39;t have to use any kind of base class, marker interface, or .Net attribute to designame any part of the behavior of the <code>CreateIssueHandler</code> class. In the <code>Handle()</code> method, the first argument is always assumed to be the message type for the handler action. It&#39;s not apparent in any of the quick start samples, but Jasper message handler methods can be asynchronous as well as synchronous, depending on what makes sense in each handler. So no littering your code with extraneous <code>return Task.Completed;</code> code like you&#39;d have to with other .Net tools.</p><p>As I mentioned earlier, we want our API to create an email whenever a new issue is created. In this case I&#39;m opting to have that email generation and email sending happen in a second message handler that will run after the initial command. You might also notice that the <code>CreateIssueHandler.Handle()</code> method returns an <code>IssueCreated</code> event. When Jasper sees that a handler creates what we call a [cascading message](TODO -- link here!), Jasper will publish the <code>IssueCreated</code> event to an in memory queue after the initial message handler succeeds. The advantage of doing this is allowing the slower email generation and sending process to happen in background processes instead of holding up the initial web service call.</p><p>The <code>IssueHandled</code> event message will be handled by this code:</p><p><a id="snippet-sample_quickstart_issuecreatedhandler"></a></p><div class="language-cs"><pre><code><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">class</span> <span class="token class-name">IssueCreatedHandler</span>
<span class="token punctuation">{</span>
    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">async</span> <span class="token return-type class-name">Task</span> <span class="token function">Handle</span><span class="token punctuation">(</span><span class="token class-name">IssueCreated</span> created<span class="token punctuation">,</span> <span class="token class-name">IssueRepository</span> repository<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        <span class="token class-name"><span class="token keyword">var</span></span> issue <span class="token operator">=</span> repository<span class="token punctuation">.</span><span class="token function">Get</span><span class="token punctuation">(</span>created<span class="token punctuation">.</span>Id<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token class-name"><span class="token keyword">var</span></span> message <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">BuildEmailMessage</span><span class="token punctuation">(</span>issue<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">using</span> <span class="token class-name"><span class="token keyword">var</span></span> client <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">SmtpClient</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        client<span class="token punctuation">.</span><span class="token function">Send</span><span class="token punctuation">(</span>message<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>

    <span class="token comment">// This is a little helper method I made public</span>
    <span class="token comment">// Jasper will not expose this as a message handler</span>
    <span class="token keyword">internal</span> <span class="token keyword">static</span> <span class="token return-type class-name">Task<span class="token punctuation">&lt;</span>MailMessage<span class="token punctuation">&gt;</span></span> <span class="token function">BuildEmailMessage</span><span class="token punctuation">(</span><span class="token class-name">Issue</span> issue<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        <span class="token comment">// Build up a templated email message, with</span>
        <span class="token comment">// some sort of async method to look up additional</span>
        <span class="token comment">// data just so we can show off an async</span>
        <span class="token comment">// Jasper Handler</span>
        <span class="token keyword">return</span> Task<span class="token punctuation">.</span><span class="token function">FromResult</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">MailMessage</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p><sup><a href="https://github.com/JasperFx/alba/blob/master/src/Samples/Quickstart/IssueCreatedHandler.cs#L5-L29" title="Snippet source file">snippet source</a> | <a href="#snippet-sample_quickstart_issuecreatedhandler" title="Start of snippet">anchor</a></sup></p><p>Now, you&#39;ll notice that Jasper is happy to allow you to use static methods as handler actions. And also notice that the <code>Handle()</code> method takes in an argument for <code>IssueRepository</code>. Jasper always assumes that the first argument of an handler method is the message type, but other arguments are inferred to be services from the system&#39;s underlying IoC container. By supporting <a href="https://betterprogramming.pub/the-3-types-of-dependency-injection-141b40d2cebc" target="_blank" rel="noopener noreferrer">method injection</a> like this, Jasper is able to cut down on even more of the typical cruft code forced upon you by other .Net tools.</p><p><em>You might be saying that this sounds like the behavior of the conventional method injection behavior of Minimal API in .Net 6, and it is. But I&#39;d like to point out that Jasper had this years before the ASP.Net team got around to it:-)</em></p><p>This page introduced the basic usage of Jasper, how to wire Jasper into .Net applications, and some rudimentary <code>Handler</code> usage. There&#39;s much more of course, so learn more about <a href="/guide/messages/">Handlers and Messages</a>.</p>`,42),p=[o];function c(i,l,r,u,d,k){return a(),n("div",null,p)}var g=s(t,[["render",c]]);export{h as __pageData,g as default};
